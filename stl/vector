#include<bits/stdc++.h>

using namespace std;
vector<int>insertvec(int arr[],int n)
{
    vector<int>v;
    {
        for(int i=0;i<n;i++)
        v.push_back(arr[i]);
   
    }
   
    return v;
    
}
int main()
{
   int t;
   cin>>t;
   while(t--)
   {
       int n;
       cin>>n;
       int arr[n];
       for(int i=0;i<n;i++)
       {
           cin>>arr[i];
       }
       vector<int>res= insertvec(arr,n);
       for(auto x:res)
       {
           cout<<x<<" ";
       }
       cout<<endl;
   }
    return 0;
}
